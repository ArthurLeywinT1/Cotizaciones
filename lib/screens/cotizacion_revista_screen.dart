import 'package:flutter/material.dart';

class CotizacionRevistaScreen extends StatelessWidget {
  const CotizacionRevistaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "3",
        style: TextStyle(fontSize: 90, fontWeight: FontWeight.bold),
      ),
    );
  }
}
